import Image from 'next/image'
import React from 'react'

export default function DoorStepSliderText({ title, color }) {
    return (
        <div>
            <h3>{title}</h3>
        </div>
    )
}
